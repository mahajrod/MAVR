#!/usr/bin/env bash
#-----------------Environment variables-----------------
FASTQ_DIR=/mnt/genomerussia/skliver/SGD_project/fastq/
RAW_READS_DIR=${FASTQ_DIR}/raw/
UNPACKED_READS_DIR=${FASTQ_DIR}/unpacked/
FILTERED_READS_DIR=${FASTQ_DIR}/filtered/

ANALYSIS_DIR=/mnt/genomerussia/skliver/SGD_project/analysis/

ALIGNMENT_DIR=${ANALYSIS_DIR}/alignment/
ALIGNMENT_BAM_DIR=${ALIGNMENT_DIR}/bam/
ALIGNMENT_LOG_DIR=${ALIGNMENT_DIR}/log/
ALIGNMENT_TMP_DIR=${ALIGNMENT_DIR}/tmp/

JF_DB_DIR=${ANALYSIS_DIR}/jf/

STAT_DIR=${ANALYSIS_DIR}/stat/
ADAPTERS_STAT_DIR=${STAT_DIR}/adapters/
FASTQC_STAT_DIR=${STAT_DIR}/fastqc/
FILTERING_STAT_DIR=${STAT_DIR}/filtering/
KMER_STAT_DIR=${STAT_DIR}/kmer/

TOOLS_DIR=/home/skliver/soft/
MAVR_SCRIPTS_DIR=${TOOLS_DIR}/MAVR/scripts/
FACUT_BIN_DIR=${TOOLS_DIR}/Facut/bin/
FASTQC_DIR=${TOOLS_DIR}/FastQC/
COOCKIECUTTER_SRC_DIR=${TOOLS_DIR}/Cookiecutter_counter/src/

ADAPTER_KMER_FILE=${TOOLS_DIR}/service_seq/trueseq_adapters_with_rev_com_23_mer.kmer
#PYTHONPATH=${PYTHONPATH}:${TOOLS_DIR}/MAVR
#export PYTONPATH
#-------------------------------------------------------

#----------------------Settings-------------------------
THREAD_NUMBER=15
KMER_SIZE=23
MEMORY=30G
PHRED_SCORE_TYPE=phred33
READ_NAME_TYPE=illumina
QUALITY_THRESHOLD=20
#-------------------------------------------------------

mkdir -p ${ANALYSIS_DIR} ${FILTERED_READS_DIR} ${JF_DB_DIR} ${STAT_DIR} ${ADAPTERS_STAT_DIR} ${FASTQC_STAT_DIR} ${FILTERING_STAT_DIR} ${KMER_STAT_DIR}

export FASTQ_DIR RAW_READS_DIR UNPACKED_READS_DIR UNPACKED_READS_DIR FILTERED_READS_DIR ANALYSIS_DIR
export JF_DB_DIR STAT_DIR ADAPTERS_STAT_DIR FASTQC_STAT_DIR FILTERING_STAT_DIR KMER_STAT_DIR
export TOOLS_DIR MAVR_SCRIPTS_DIR FACUT_BIN_DIR FASTQC_DIR COOCKIECUTTER_SRC_DIR ADAPTER_KMER_FILE
export THREAD_NUMBER KMER_SIZE MEMORY PHRED_SCORE_TYPE READ_NAME_TYPE QUALITY_THRESHOLD